
Experiment in publishing ontodocs that describe parts of glue.ai, using jOWL.

Data can be pointed to using SVN-extern mounts from subprojs like appdapter + cogchar, 
or explicit SVN-ref URLs in the HTML.